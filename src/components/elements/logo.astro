---
import { clsx } from 'clsx/lite'

interface Props {
  class?: string
  [key: string]: any
}

const { class: className, ...props } = Astro.props
---
<span class={clsx('flex h-8 items-stretch', className)} {...props}>
  <slot />
</span>
