---
import { clsx } from 'clsx/lite'

interface Props {
  class?: string
  [key: string]: any
}

const { class: className, ...props } = Astro.props
---
<main class={clsx('isolate overflow-clip', className)} {...props}>
  <slot />
</main>
